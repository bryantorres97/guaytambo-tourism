import { ImagenPortadaPipe } from './imagen-portada.pipe';

describe('ImagenPortadaPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenPortadaPipe();
    expect(pipe).toBeTruthy();
  });
});
